{
  "name": "@eve/profile-renderer",
  "version": "0.1.0",
  "private": true,
  "description": "Profile-aware entity renderer host for Eve OS. Mirror of @synap-core/profile-renderer with the RendererRef shape inlined (no @synap-core/renderer-runtime dep in Eve's workspace).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.5.0"
  }
}
