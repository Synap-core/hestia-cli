{
  "name": "@eve/profile-renderer",
  "version": "0.1.0",
  "private": true,
  "description": "Profile-aware entity renderer for Eve OS. Hosts `<EntityRenderer>` (resolver + dispatch) and `<EntityDetailRenderer>` (the built-in HeroUI detail UI with schema-driven fields, relations panel, and multi-column layout). Mirror of @synap-core/profile-renderer; types inlined to avoid the Synap workspace dep.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@eve/fields": "workspace:*"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "@heroui/react": "^2.4.0",
    "lucide-react": "^0.441.0"
  },
  "devDependencies": {
    "@heroui/react": "^2.4.0",
    "@types/react": "^19.0.0",
    "lucide-react": "^0.441.0",
    "typescript": "^5.5.0"
  }
}
